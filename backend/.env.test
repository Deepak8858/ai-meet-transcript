# Test Environment Configuration
# This file contains configurations for testing environment

# Server Configuration
PORT=5001
NODE_ENV=test

# Groq API Configuration - Use test/staging keys
GROQ_API_KEY=test_groq_api_key

# Google Gemini API (Test)
GEMINI_API_KEY=test_gemini_api_key

# Gmail SMTP Configuration (Test)
GMAIL_USER=test@example.com
GMAIL_APP_PASSWORD=test_app_password

# Email Configuration
EMAIL_FROM_NAME=AI Meeting Notes Summarizer (Test)
EMAIL_FROM_ADDRESS=test@example.com
EMAIL_REPLY_TO=test@example.com

# SMTP Configuration (Test)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=test@ethereal.email
SMTP_PASS=test_password

# Rate Limiting (Relaxed for testing)
RATE_LIMIT_WINDOW_MS=10000
RATE_LIMIT_MAX_REQUESTS=1000

# File Upload Configuration (Test)
MAX_FILE_SIZE=5242880
MAX_CONTENT_SIZE=51200
ALLOWED_FILE_TYPES=.txt,.md,.json,.csv

# CORS Configuration
CORS_ORIGIN=http://localhost:3001

# Supabase Configuration (Test)
SUPABASE_URL=https://test.supabase.co
SUPABASE_ANON_KEY=test_anon_key
SUPABASE_SERVICE_KEY=test_service_key

# Logging Configuration
LOG_LEVEL=silent
GROQ_LOG=silent

# Security Configuration
BCRYPT_ROUNDS=4
JWT_SECRET=test_jwt_secret
SESSION_SECRET=test_session_secret

# Database Configuration (Test)
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/meeting_notes_test_db

# Redis Configuration (Test)
REDIS_URL=redis://localhost:6379/1

# AI Model Configuration (Test)
DEFAULT_MODEL=llama3-8b-8192
FALLBACK_MODEL=mixtral-8x7b-32768
MAX_TOKENS=1000
TEMPERATURE=0.5
TOP_P=0.8

# Test Configuration
TEST_TIMEOUT=30000
TEST_RETRIES=3
MOCK_EXTERNAL_SERVICES=true

# Email Templates (Test)
EMAIL_TEMPLATE_DIR=./test/templates/email